apiVersion: v1
kind: Service
metadata:
  name: employee-service
  namespace: microservices
  labels:
    app: employee-service
spec:
  type: ClusterIP
  selector:
    app: employee-service
  ports:
    - name: http
      port: 8081
      targetPort: 8081
