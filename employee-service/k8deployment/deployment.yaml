apiVersion: apps/v1
kind: Deployment
metadata:
  name: employee-service
  namespace: microservices
  labels: #
    app: employee-service
spec:
  replicas: 1 #kept at 1 - the in-memory repository isn't shared across pods, so a second replica would silently diverge (writes to one pod invisible to the other); see INTERVIEW-NOTES.md
  revisionHistoryLimit: 5 #
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: employee-service
  template:
    metadata:
      labels:
        app: employee-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8081"
        prometheus.io/path: "/actuator/prometheus"
    spec:
      containers:
        - name: employee-service
          #image: employee-service:latest
          image: localhost:5000/employee-service:latest #Here we are pushing employee-service docker image into local docker repository
          imagePullPolicy: Always #Always #Never # this is used for pull image every time from local docker repository not remote docker repository when we build docker employee-service image
          ports:
            - containerPort: 8081
          env:
            - name: EUREKA_URL
              valueFrom:
                configMapKeyRef:
                  name: microservices-config
                  key: eureka.url
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              valueFrom:
                configMapKeyRef:
                  name: microservices-config
                  key: otel.endpoint
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8081
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8081
            initialDelaySeconds: 40
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: employee-service
  namespace: microservices
spec:
  selector:
    app: employee-service
  ports:
    - port: 8081
      targetPort: 8081
  type: ClusterIP
